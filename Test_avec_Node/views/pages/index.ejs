<!DOCTYPE html>
<html>
	<head>
		<% include ../partials/head %>
            <title>Home</title>
	</head>
    <body>
        <% include ../partials/header %>
        
        <div class="container">
            <% include ../partials/msg %>
            <div class="index">
                <h1>Principe du jeu</h1>
                <p>En début de partie, le joueur est matérialisé sur la cellule d’entrée du labyrinthe.</p>
                <p>Il peut se déplacer avec les touches directionnelles du clavier, mais ne peut pas franchir les murs.</p>
                <p>Lorsque le joueur parvient à la sortie, ou que le temps est écoulé, cela déclenche la fin de partie.</p>
            </div>
            <!--if (!user)-->
            <form action="/login" class="form-signin" method="post">
                <h2 class="form-signin-heading">Log in</h2>
                <label for="inputUsername" class="sr-only">User name</label>
                <input name="username" type="text" id="inputUsername" class="form-control" placeholder="User name" required autofocus>
                <label for="inputPassword" class="sr-only">Password</label>
                <input name="password" type="password" id="inputPassword" class="form-control" placeholder="Password" required>
                <div class="checkbox">
                    <label>
                        <input name="remember" type="checkbox" value="remember-me"> Remember me
                    </label>
                </div>
                <button class="btn btn-lg btn-success btn-block" type="submit">Log in</button>
            </form>
            <p>Don't have an account yet? <a href="/signup">Register</a> instead</p>
           
            <ul id="list"></ul>
        </div> <!-- /container -->
        

        <script>
           /* $.ajax({
                url: "/users"
            }).done(function(data) {
                console.log(data);
                for (i in data){
                $('#list').append('<li>'+data[i].username+' '+data[i].email+'</li>');
                }
            }).fail(function(req,text_status,err){
                console.log(text_status);
                 $('#list').append('<li>Unregistered</li>');
            });*/
        </script>
        <footer><% include ../partials/footer %></footer>
    </body>
</html>